export interface ReservationLeaseRequest {
  reservationId: string;
  organizationId: string;
  lease: string;
}

export interface ReservationLeaseResponse {
  reservationId: string;
  organizationId: string;
  lease: string;
}


