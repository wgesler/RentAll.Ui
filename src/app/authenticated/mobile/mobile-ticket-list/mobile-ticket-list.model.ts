export type MobileTicketListDisplay = {
  ticketId: string;
  title: string;
  property: string;
};

export type MobileTicketListRow = {
  ticketId: string;
  ticketAttentionDot: string;
  ticketCode: string;
  title: string;
};
